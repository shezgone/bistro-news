# docs/spec/ — 기획팀 선행작업 원본 (gitignore)

권위 스펙·실험 원본. **저작권·용량으로 git 추적 제외** (이 README만 추적). 로컬에만 보관.

| 파일 | 내용 |
|---|---|
| `뉴스인과관계분석_기획팀_선행작업.pdf` | "Track 2. 뉴스 인과관계 파이프라인 분석" — 권위 스펙. 충돌 시 우선(CLAUDE.md 참조). |
| `뉴스 인과관계 분석.xlsx` | LLM IE 실험 원본. 시트 3개: `기존 프롬포트` / `신규프롬포트` / `간이 비교 평가`. `eval/extract_from_xlsx.py` 의 인제스트 소스. |

> 프롬프트 원문은 `prompts/step_*.md` 로, 비교 출력은 `eval/predictions/` 로 이관됨 — 이 원본 없이도 파이프라인·평가 재현 가능.
